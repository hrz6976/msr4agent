This directory stores committed manifest files for local experiment dependencies.

`repositories.jsonl` is the source of truth for the repositories we check out into `repo/`.
Each line records the upstream URL and the exact pinned revision we use.
