### 软件开发上下文

**一、 背景认知**

- **代码拓扑图谱：** ~~跨文件调用图、类继承关系、依赖库版本及抽象语法树结构。CKG~~
- **业务需求映射：** 需求文档、工单描述、Bug报告及复现步骤，以及 Commit Message 与对应 Issue/PR 的语义关联。
- **制度化规约：** 给人读的和给机器读的规范文件（例如：包含架构设计原则、贡献指南、构建和测试命令，或“必须使用单引号”等代码风格的 `AGENTS.md` 或 `.cursorrules` 文件）。should be done?

**二、 演进经验**

- **变更耦合与隐式依赖：** 历史高频同时修改的文件集（例如：发现 `src/qmd.ts` 与 `src/llm.ts` 共变频率极高，或业务代码与对应测试用例经常打包提交，从而引导Agent主动同步更新）。
- **历史缺陷与修复模式：** “Bug报告 -> 错误代码 -> 修复补丁”的完整演进链路（例如：学习项目特有的异常捕获策略或边界处理）。过去未合并的失败 PR 及其审查意见可作为“负面上下文”防止 Agent 踩坑。
- **代码健康度与风险感知：** 文件修改频率、代码复杂度趋势及测试覆盖率波动（例如：当 Agent 准备修改历史变更频繁且易错的“热点”文件时，强制要求其先执行重构或生成更多单元测试）。
- **运行时执行轨迹：** CI/CD 构建失败的报错日志、堆栈跟踪（Stack Traces）和性能 Profiling 数据。

**三、 协作语境**

- **代码审查与协作标准：** 挖掘 PR 讨论线程及明确的质量红线（例如：PR 必须包含测试用例和更新的文档；团队特有的编程偏好）。
- **架构决策动机：** 历史 Issue 讨论、长文本 PR 描述及架构决策记录（例如：在 Agent 尝试重构冗余代码时，提示这是一个为兼容特定旧版本客户端而刻意保留的 Workaround，防止引入逻辑回归）。
- **版本管理规范：** 历史分支策略与提交规范（例如：功能开发使用 `feature/` 分支，合并到 `develop` 分支）。
- **开发者专长地图：** 基于模块提交量与复杂 Issue 解决记录构建的人员能力库（例如：Agent 遇到极度缺乏上下文的架构决策时，明确知道该 `@` 哪位人类专家求助，或在提交 PR 时自动分配最合适的 Code Reviewer）。
